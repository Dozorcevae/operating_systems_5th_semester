project('lab6', 'c',
  version: '1.0',
  default_options: ['warning_level=3', 'c_std=c11'])

srcdir = 'src'

executable('merge_sorted', join_paths(srcdir, 'merge_sorted.c'))
executable('remove_long_lines', join_paths(srcdir, 'remove_long_lines.c'))

